# workers

Worker code has been split out of this UI repo.

- Hosted Cloudflare Worker API: `../morning-api`
- Private/tailnet discovery and extraction workers: `../morning-fetchers`

This repo now contains the React UI and legacy/temporary Node APIs only.
